gdn: 1
id: study-flashcards
name: Study Flashcards
description: Cycles through term/definition flashcards from any JSON deck you host — build your own study set for any subject.
author: jkapera
width: 128
height: 32
refresh: 600
pages: [subject, term, definition]
inputs:
  - key: deckurl
    type: string
    app_input_type: free-text
    label: Deck URL (no https://)
    default: ""
    help: "Host a JSON array of {term, definition} objects anywhere public (GitHub raw works great) and paste the URL here, without https://"