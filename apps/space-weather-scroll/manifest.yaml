gdn: 1
id: space-weather-scroll
version: 0.1.0
name: Solar Flares
author: glance-led-dev
description: The current GOES X-ray flux and flare class, and what it means for radio.
category: Science
entry: app.star

width: 192
height: 32
refresh: 3600

pages: [flux]

# No assets: the sun is baked into app.star as a sprite string, so it can be
# recoloured per state and never needs scaling at render time.
